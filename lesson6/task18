1. Написать функцию которая принимает один обязательный аргумент и один дефолтный со значением 7 затем возвращает их
произведение.
Вызвать функцию с передачей дефолтного аргумента и без передачи дефолтного аргумента.

2. Написать функцию принимающую 2 списка. Один из них дефолтный. Функция должна возвращать сумму списков.
Вызвать функцию с передачей дефолтного аргумента и без передачи дефолтного аргумента.

3. Написать функцию принимающую 2 числа (одно из них дефолтное) и дефолтный словарь. Функция должна возвращать сумму
чисел разделенную на значение под ключём 'divisor' из словаря.
Вызвать функцию с передачей дефолтного аргумента и без передачи дефолтного аргумента.
